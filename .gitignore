node_modules/
dist/
.env
.env.local
data/incident.json
# Copyrighted FDNY doctrine corpus index — LOCAL ONLY, never committed,
# never bundled into the client or any public build. Tactics cards are
# derived from it and stay local for the same reason.
server/data/doctrine/
server/data/tactics/
# Runtime coordination state (NYCEM EOC/requests/ticker) + saved exercise AARs
server/data/nycem-state.json
server/data/weather-state.json
server/data/exercises/
.DS_Store
*.log
*.tsbuildinfo
