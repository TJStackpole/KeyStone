node_modules/
dist/
.env
.env.local
data/incident.json
# Copyrighted FDNY doctrine corpus index — LOCAL ONLY, never committed,
# never bundled into the client or any public build.
server/data/doctrine/
.DS_Store
*.log
*.tsbuildinfo
